# HyperLink Navigator

HyperLink Navigator 是一个面向技术调研、内容聚合与垂直领域资源导航的开源工具集。项目定位为轻量级外链管理与展示系统，主要服务于需要快速构建主题资源索引站点的开发者、内容运营者及技术文档维护团队。通过结构化的数据配置与静态站点生成逻辑，本项目帮助用户将散落的网络链接转化为具备分类体系、检索能力和展示层级的可维护资源库，有效解决信息碎片化、链接失效追溯困难以及资源复用率低下的实际问题。

本项目不提供爬虫或自动化采集功能，专注于人工精选链接的手动录入、标签化组织与多终端适配展示。其核心设计理念为“配置即导航”，用户仅需维护若干 JSON 或 YAML 格式的数据文件，即可驱动完整的资源展示前端界面，并自动生成对应的 sitemap 与 RSS 订阅源，便于搜索引擎收录与用户订阅跟踪。项目内置模板引擎支持自定义卡片布局、列表视图与紧凑表格三种展示模式，满足从个人书签管理到团队知识库外链聚合的不同粒度需求。

## 功能概览

- **多级分类树管理**：支持无限层级的分类嵌套，每个分类可单独设置图标、描述、排序权重及访问权限标识，便于构建如“技术文档 / 前端框架 / React 生态”等多级导航路径。

- **链接字段扩展体系**：除标题、URL、描述等基础字段外，提供自定义属性面板，用户可添加“维护状态”、“语言”、“收费模式”、“推荐指数”等业务标签，并支持按任意扩展字段过滤与排序。

- **静态站点生成引擎**：基于配置文件直接输出完整的 HTML 页面集合，无需后端服务即可部署至任意静态托管平台。生成过程包含资源去重校验、失效链接预检（基于 HTTP 状态码）及相对路径转换。

- **链接存活监控仪表板**：集成轻量级定时检测任务，通过配置 cron 表达式周期性检查已收录链接的可访问性，并在管理后台以红黄绿灯状态标识展示，辅助用户及时清理或更新失效资源。

- **全文检索引擎**：内置倒排索引机制，支持对链接标题、描述、分类路径及自定义标签进行中文分词检索，检索响应时间控制在 200 毫秒以内，适配千级链接规模。

- **导入导出适配器**：提供 CSV、Markdown 列表、浏览器书签 HTML 及 JSON 多种格式的批量导入导出接口，便于与现有工具链（如 Notion、Raindrop、Pocket）进行数据交换。

- **访问统计与热度排行**：基于页面浏览量（PV）与用户点击事件，自动生成热门链接周榜与月榜，帮助运营者识别高价值资源并调整推荐位。

## 应用场景

- **技术团队内部知识库外链整合**：研发团队可将日常使用的 API 文档、设计规范、代码仓库模板、CI/CD 最佳实践等外部链接统一纳入 HyperLink Navigator 管理，按项目组或技术栈分门别类，新人入职时仅需访问导航首页即可获取全部必要外部资源，显著降低信息传递成本。

- **垂直领域内容站点资源聚合页**：例如编程语言社区、设计素材分享站或学术论文索引站，站点维护者可利用本项目的分类树和扩展字段，构建面向特定受众的精选资源目录，配合静态生成能力，无需数据库即可支撑日均万级访问量。

- **个人书签管理与跨设备同步**：用户将本项目部署至个人云存储或低代码托管平台后，通过导入浏览器书签导出文件，获得一套具备搜索、分类和状态监控的自托管书签系统，彻底摆脱浏览器厂商的同步限制与隐私顾虑。

- **开源项目文档站的外部链接附录**：开源项目维护者可在项目文档中嵌入由 HyperLink Navigator 生成的“相关生态”或“致谢引用”页面，将分散在 Issue、讨论区及 README 中的外部引用集中展示，并利用存活监控功能确保所有引用链接长期有效。

## 快速开始

以下命令序列适用于 Linux / macOS / Windows WSL 环境，假设系统已安装 Git 与 Node.js 运行时（版本要求详见安装要求章节）。

```bash
# 克隆项目仓库至本地
git clone https://github.com/hyperlink-navigator/hln-core.git

# 进入项目工作目录
cd hln-core

# 安装项目依赖（使用 npm 或 yarn）
npm install

# 复制示例配置文件并进行初始定制
cp config/example.config.yaml config/default.config.yaml

# 执行静态站点生成，输出目录默认为 ./dist
npm run build

# 启动本地预览服务，访问 http://localhost:8080 查看生成的导航站点
npm run serve
```

首次运行完成后，用户可编辑 `config/default.config.yaml` 中的链接数据源路径与分类定义，并重新执行 `npm run build` 以应用变更。生产环境部署时，建议将 `./dist` 目录整体上传至 Nginx、Caddy 或云厂商对象存储的静态网站托管服务。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行构建脚本与本地服务；不支持 16.x 及以下版本，因依赖原生 Fetch API 与 Watch 模式 |
| npm | 9.x 或以上 | 包管理器，用于安装项目依赖；若使用 yarn 1.22+ 或 pnpm 8.x 亦可，但需自行适配 lock 文件 |
| Git | 2.30 或以上 | 版本控制工具，用于克隆仓库及后续拉取更新；Windows 用户需确保启用符号链接支持 |
| 内存 | 最低 512 MB，推荐 1 GB | 构建时链接数量超过 5000 条时，内存占用峰值可能达到 1.2 GB，建议根据实际数据规模调整 Node.js 内存限制参数 |
| 磁盘空间 | 不低于 200 MB | 包含依赖安装、构建缓存及输出文件；若启用链接快照截图功能，需额外预留每张截图约 50 KB 空间 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 入门指南 | /docs/getting-started.md | 如何从零开始配置第一个导航站点？分类与链接的数据结构是怎样的？如何切换卡片/列表/表格三种展示模板？ |
| 配置手册 | /docs/configuration.md | 全局配置文件中的所有字段含义、类型及默认值；环境变量覆盖规则；多环境配置拆分策略（开发/预发布/生产） |
| 扩展开发 | /docs/extending.md | 如何编写自定义字段渲染器？如何增加新的导入导出格式？如何替换内置的图标库或分词器？ |
| API 参考 | /docs/api/ | 核心模块（Parser、Indexer、Generator、Monitor）的 TypeScript 类型定义与公共方法签名，适用于二次开发或深度集成 |

## 资源列表

下列链接为本项目初始收录的部分垂直领域参考资源，按主题归类展示。所有链接均保留用户提供的原始格式，未做任何协议补全或域名规范化处理。

**综合导航类**

- <code>huangjinwangzhanappmianfeidaquangangguo.com.cn</code>

**垂直主题类**

- <code>yslmitaoseshencai.com.cn</code>
- <code>yslmitaoserenqi.com.cn</code>
- <code>yslmitaoseshipin.com.cn</code>
- <code>yslmitaosejuru.com.cn</code>
- <code>yslmitaosezaixian.com.cn</code>

## 项目结构

```
hln-core/
├── bin/                                 # 命令行入口脚本
│   └── hln.js                           # 主 CLI 程序，解析子命令（build/serve/check/import）
├── config/                              # 配置目录
│   ├── default.config.yaml              # 默认全局配置（分类树、输出路径、语言、分页大小）
│   └── example.data.json                # 示例链接数据，展示字段结构与嵌套分类用法
├── src/                                 # 源代码目录
│   ├── core/                            # 核心逻辑层
│   │   ├── parser/                      # 配置与数据解析器，支持 YAML/JSON/CSV 格式识别与校验
│   │   ├── indexer/                     # 倒排索引构建模块，含中文分词器与布尔查询解析器
│   │   ├── generator/                   # 静态页面生成引擎，基于 EJS 模板与异步并行渲染
│   │   └── monitor/                     # 链接存活监控模块，含 HTTP 客户端池与状态持久化
│   ├── templates/                       # 内置展示模板
│   │   ├── layouts/                     # 基础布局模板（header/footer/sidebar）
│   │   └── partials/                    # 可复用组件（分类面包屑、链接卡片、分页控件、搜索框）
│   ├── utils/                           # 通用工具函数
│   │   ├── validator.js                 # URL 格式校验、相对路径转换、文件名安全化
│   │   └── logger.js                    # 分级日志输出（info/warn/error），支持 JSON 格式与彩色终端
│   └── types/                           # TypeScript 类型定义
│       ├── config.d.ts                  # 配置结构接口
│       └── link.d.ts                    # 链接及扩展字段数据结构
├── test/                                # 单元测试与集成测试
│   ├── unit/                            # 各模块独立测试用例（覆盖率目标 80%）
│   └── fixtures/                        # 测试用固定数据集（含模拟链接与分类树）
├── dist/                                # 默认构建输出目录（gitignore 忽略）
├── logs/                                # 运行日志存储目录（按日期滚动，保留最近 30 天）
├── docs/                                # 用户文档与开发文档（Markdown 格式）
├── .eslintrc.cjs                        # ESLint 代码规范配置（基于 Airbnb 风格定制）
├── .prettierrc                          # Prettier 格式化配置（单引号、尾逗号 all）
├── package.json                         # 项目元信息与依赖声明
├── tsconfig.json                        # TypeScript 编译选项（target ES2022，strict 模式）
└── README.md                            # 项目总体说明（即当前文档）
```

## 贡献指南

1. 查阅 issue 列表或讨论区，确认待处理任务或建议的新功能未被他人认领。对于较大改动（如新增数据源适配器或修改核心索引算法），建议先创建讨论议题并附上设计简述，以获得核心维护者的反馈方向。

2. 派生项目仓库至个人账户，在派生副本中创建功能分支，分支命名遵循 `<类型>/<简述>` 格式，例如 `feat/add-rss-output` 或 `fix/memory-leak-on-large-dataset`。

3. 编码过程中请遵守项目已配置的 ESLint 与 Prettier 规则，并为新增或修改的公共函数编写对应的单元测试用例，测试文件置于 `test/unit/` 下，命名与源文件对应。

4. 提交变更时使用约定式提交消息格式（`<类型>: <描述>`），类型包括 feat、fix、docs、style、refactor、perf、test、chore。提交前确保本地 `npm run test` 与 `npm run build` 全部通过，无新增警告。

5. 发起合并请求至主仓库的 `main` 分支，在请求描述中清晰列出变更内容、影响范围及手动测试步骤。合并请求至少需要一名维护者审阅通过，且所有 CI 检查（测试覆盖、构建、链接检查）均呈绿色状态后方可合入。

## 常见问题

**问：生成的静态站点是否支持多语言界面？如何配置？**

答：目前版本内置英文与简体中文两套界面语言包，切换方式为在全局配置文件中设置 `locale: zh-CN` 或 `locale: en-US`。所有模板中的静态文本（如“搜索”、“分类”、“热门排行”）均通过 i18n 键值对引用，用户可自行扩展 `src/locales/` 目录下的 JSON 文件以增加其他语言。动态内容（链接标题、描述、分类名称）的语言由数据文件内容决定，系统不自动翻译。

**问：链接存活监控会频繁请求外部站点，是否会被目标服务器封禁？**

答：监控模块默认采用单线程顺序请求，并内置 1.5 秒至 5 秒的随机间隔抖动（符合 robots.txt 的 Crawl-delay 建议）。同时，请求头中的 User-Agent 字段设置为 `HyperLinkNavigator-Monitor/1.0` 且不携带 Cookie，仅执行 HEAD 方法获取状态码，不下载响应体，以最大程度降低对目标服务器的负载。若需监控内部私有域名，可在配置中关闭 SSL 证书验证或自定义 DNS 解析器。

> 外链数量: 6 | 生成时间: 2026-08-25 23:22:11
