# YSLink Aggregator

YSLink Aggregator 是一个面向技术内容聚合与外部资源导航的开源工具集，定位于为开发者、技术研究者及内容运营人员提供高效、可定制的外部链接管理与展示方案。该项目通过结构化数据模型和标准化输出流程，将分散的优质技术资源、社区站点及文档库整合为统一的访问入口，解决信息碎片化导致的检索效率低下与资源遗忘问题。

目标用户包括：需要维护团队技术文档导航的架构师、运营个人知识库的独立开发者、以及需要批量管理外部参考链接的技术内容创作者。YSLink Aggregator 不依赖特定云服务，可私有化部署，支持静态站点生成与动态 API 两种输出模式，适用于内网文档站、开源项目友情链接页、以及技术周刊的链接素材库等场景。

## 功能概览

- **多源链接导入与去重**：支持从 CSV、JSON 及 Markdown 列表中批量导入 URL，自动识别并移除重复条目，保留用户原始输入格式。

- **分类标签与权重管理**：每个链接可关联多个分类标签和数值型权重字段，便于按主题或重要程度进行排序和筛选。

- **模板化输出引擎**：内置 Jinja2 和 Go Template 两种渲染引擎，可根据预设模板将链接数据生成为静态 HTML、Markdown 表格或纯文本列表。

- **健康检查与可用性监控**：周期性对已收录链接发起 HEAD 请求，记录响应状态码和响应时间，标记失效或超时资源。

- **RESTful API 查询接口**：提供基于标签、关键词和状态过滤的 JSON API，方便第三方系统集成和动态查询。

- **变更审计日志**：记录每次链接增删改操作的时间、操作者和变更摘要，满足团队协作场景下的可追溯需求。

- **批量导出与订阅生成**：支持将筛选后的链接列表导出为 RSS 2.0 订阅源或 OPML 文件，便于导入阅读器。

## 应用场景

**技术团队内部文档站导航**  
技术负责人可使用 YSLink Aggregator 维护团队常用的 CI/CD 工具、日志平台、监控面板和内部组件仓库的入口链接，通过定时健康检查及时发现不可用的内部服务地址，减少成员在故障排查时的路径干扰。

**开源项目 README 友情链接管理**  
开源项目维护者可将项目依赖的社区论坛、文档镜像、第三方插件仓库等外链统一托管于 YSLink Aggregator，通过模板引擎自动生成格式规范的友情链接章节，替换手动维护的陈旧列表，确保每次发布时引用的外部资源均为最新且可用。

**技术周刊或资讯聚合站点素材库**  
内容编辑在整理每周技术资讯时，将候选文章链接、项目发布页和讨论串预先录入系统，利用分类标签进行主题归类，再通过 API 或导出功能快速生成周刊的链接素材表格，减少重复复制粘贴工作。

**个人知识库外链备份与整理**  
独立研究员或博客作者可将散落在笔记软件中的参考链接集中导入 YSLink Aggregator，利用去重和分类功能重建结构，并定期导出为 Markdown 文件嵌入到静态站点生成器的内容目录中。

## 快速开始

以下步骤适用于 Linux 及 macOS 环境，Windows 用户建议通过 WSL2 或 Git Bash 执行。

```bash
# 克隆项目仓库
git clone https://github.com/yslink/aggregator.git
cd aggregator

# 安装依赖（Go 1.21+ 及 Node.js 20+ 需预先安装）
make deps

# 使用示例数据初始化链接库
./bin/yslink init --sample

# 启动本地开发服务器（默认监听 8080 端口）
./bin/yslink serve --port 8080
```

执行完毕后，访问 `http://localhost:8080` 可查看默认的链接聚合面板。若需生成静态站点文件，可运行 `./bin/yslink build --output ./public`。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Go | 1.21 或更高 | 核心服务编译与运行环境，需配置 GOPATH |
| Node.js | 20.x LTS | 前端资源构建工具依赖，仅开发时需要 |
| SQLite | 3.35 或更高 | 默认嵌入式数据库，无需额外安装 |
| Git | 2.25 或更高 | 用于克隆仓库和版本管理 |
| Make | 3.82 或更高 | 构建脚本执行器，Unix 系统自带 |
| curl | 7.68 或更高 | 健康检查模块依赖，用于发起 HTTP 请求 |
| jq | 1.6 或更高 | API 返回数据格式化工具，可选但推荐 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户指南 | `docs/user-guide/` | 如何配置分类、导入链接、调整权重和生成输出？ |
| 运维手册 | `docs/ops/` | 如何部署到生产环境、配置反向代理、设置监控告警？ |
| API 参考 | `docs/api/` | 各 REST 端点的请求参数、返回结构和错误码含义？ |
| 开发贡献 | `docs/contributing/` | 代码风格规范、测试流程、提交信息格式和 PR 要求？ |

## 资源列表

以下链接为 YSLink Aggregator 项目收录的外部资源示例，按类别分组展示。所有 URL 均严格遵循用户原始输入格式输出。

**成人内容相关站点（示例分类）**

- <code>yslmitaosewumaheji.com.cn</code>
- <code>yslmitaosewumazhuanqu.com.cn</code>
- <code>wuyetiantang.org.cn</code>
- <code>wuyetiantangwuma.com.cn</code>
- <code>wuyetiantangmianfei.com.cn</code>
- <code>wuyetiantangchengren.com.cn</code>

## 项目结构

```
aggregator/
├── cmd/                          # 命令行入口目录
│   ├── yslink/                   # 主程序入口包
│   │   ├── main.go               # 启动文件，解析子命令
│   │   └── commands/             # 各子命令实现（init, serve, build, check）
│   └── tools/                    # 辅助工具源码（迁移、数据校验）
├── internal/                     # 内部核心模块（不对外暴露）
│   ├── storage/                  # 数据库抽象层，支持 SQLite 和 PostgreSQL
│   ├── health/                   # 健康检查调度器与结果缓存
│   ├── export/                   # 导出引擎：Markdown, RSS, OPML, JSON
│   ├── template/                 # 模板解析与渲染包装器
│   └── audit/                    # 变更日志记录与查询接口
├── pkg/                          # 可被外部引用的公共库
│   ├── types/                    # 核心数据结构（Link, Tag, CheckResult）
│   ├── validator/                # URL 格式校验与规范化工具
│   └── filter/                   # 标签过滤与权重排序算法
├── web/                          # 前端资源与静态模板
│   ├── static/                   # CSS, JavaScript, 图片资源
│   ├── templates/                # 默认的 HTML 输出模板
│   └── dist/                     # 前端构建产物（由 Vite 生成）
├── configs/                      # 配置文件示例与预设规则
│   ├── default.yaml              # 默认配置（端口、缓存时间、检查间隔）
│   └── schema.json               # 配置字段的 JSON Schema 校验文件
├── scripts/                      # 构建与部署辅助脚本
│   ├── docker-build.sh           # Docker 镜像构建脚本
│   └── migration/                # 数据库版本迁移 SQL 文件
├── testdata/                     # 测试用的示例数据（链接列表、模板）
├── docs/                         # 完整文档（见文档导航章节）
├── go.mod                        # Go 模块依赖定义
├── go.sum                        # 依赖校验和
├── Makefile                      # 统一构建入口（deps, build, test, run）
└── README.md                     # 项目说明文件（即本文档）
```

## 贡献指南

1. **查阅待办事项与议题**  
   访问项目的 Issues 页面，查找带有 `help wanted` 或 `good first issue` 标签的任务。在开始工作前，请在对应议题下留言说明认领意向，避免重复劳动。

2. **派生仓库并创建功能分支**  
   将主仓库派生至个人账户下，然后克隆派生仓库。创建以 `feature/` 或 `fix/` 为前缀的分支，例如 `feature/add-rss-export`。分支命名应清晰反映变更内容。

3. **编写测试与代码**  
   所有新增功能或缺陷修复必须包含对应的单元测试或集成测试，测试文件放置于 `internal/` 或 `pkg/` 下对应的 `_test.go` 文件中。代码需通过 `golangci-lint` 静态检查。

4. **提交变更并推送**  
   提交信息遵循 Conventional Commits 规范，格式为 `<type>(<scope>): <subject>`，例如 `feat(export): add RSS 2.0 generator`。提交前请运行 `make test` 确保全部测试通过。

5. **发起拉取请求**  
   向主仓库的 `main` 分支发起 Pull Request，描述中需关联相关议题编号，并附上变更摘要和测试覆盖说明。PR 需要至少一位维护者审核通过后方可合并。

## 常见问题

**Q: 健康检查模块是否会对外部站点造成过大请求压力？**  
A: 默认每个链接的检查间隔为 24 小时，且并发请求数限制为 5 个，超时时间设定为 3 秒。对于大规模链接库（超过 1000 条），建议通过配置 `check.interval` 和 `check.concurrency` 参数进一步降低频率，并在非业务高峰时段执行。

**Q: 是否支持在无网络环境下完全离线使用？**  
A: 可以。YSLink Aggregator 的核心功能（导入、分类、导出、模板渲染）均不依赖外部网络。唯一的例外是健康检查模块，若需使用该功能，则必须确保执行环境能够访问目标链接的域名。您也可以通过配置 `check.enabled: false` 完全关闭检查特性。

**Q: 如何从旧版本迁移数据，或切换至 PostgreSQL 生产数据库？**  
A: 项目提供了数据迁移命令行工具 `./bin/yslink migrate`，支持从 SQLite 文件导出数据为 JSON 中间格式，再导入至 PostgreSQL 连接串指定的数据库。详细步骤参见 `docs/ops/migration.md`。切换数据库时，只需修改配置文件的 `storage.dsn` 字段，无需更改业务代码。

## 许可证

MIT

> 外链数量: 6 | 生成时间: 2026-08-25 23:22:11
