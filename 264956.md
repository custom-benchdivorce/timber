# Nova Index

Nova Index 是一个面向技术调研、内容聚合与知识管理场景的轻量化外链索引与资源导航系统。它不产生内容，不构建数据库，不依赖后端运行时状态，仅通过结构化 Markdown 与静态托管方式，为团队或社区提供高可读性、高可维护性的统一入口管理方案。

本项目定位为“技术资源的外链汇总中间层”，适用于需要频繁更新外部参考链接、但又不想维护复杂 CMS 或动态站点的场景。目标用户包括开源文档维护者、技术社区运营人员、调研团队以及个人知识库管理者。Nova Index 通过明确的目录规范与索引模板，将散落的链接转化为可版本化、可审阅、可协作的资产。

## 功能概览

- **多级索引目录生成**：基于项目结构自动生成层级化的资源导航目录，支持按主题、来源、用途等多维度分类，降低链接查找成本。

- **纯静态资源引用**：所有外链以明文形式记录于 Markdown 文件中，无 JavaScript 重定向、无后端代理，确保链接透明可追溯。

- **版本化变更记录**：利用 Git 进行链接增删改的版本管理，每次变更均附带提交信息与责任人，便于回溯历史状态。

- **自动化链接可用性检查**：内置基于定时任务的链接存活检测脚本（可选），定期报告失效链接，减少维护负担。

- **模板化资源录入**：提供统一的资源描述模板，包含标题、摘要、标签、维护状态等字段，保证条目质量一致性。

- **多格式导出支持**：索引数据可导出为 JSON、CSV 或 HTML 摘要页，便于集成至其他系统或生成静态站点。

- **访问频次统计接口**：通过解析访问日志（如 Nginx 日志），可生成各资源链接的点击热度排名，辅助内容优化决策。

## 应用场景

- **技术文档库的外部参考管理**：当项目文档需要引用大量第三方规范、SDK 地址、社区教程时，Nova Index 可作为独立的外链附录，与主文档解耦，便于独立更新与审校。

- **社区资源聚合页**：技术社区或开源组织可使用 Nova Index 维护“社区推荐工具”“常用学习站点”“镜像源列表”等页面，社区成员可通过 Pull Request 提交新链接。

- **内部调研知识库**：企业技术预研团队在评估多个竞品或方案时，可通过 Nova Index 集中存放调研对象的官网、技术白皮书、代码仓库等链接，并附加评估备注，形成可共享的调研索引。

- **个人书签管理系统**：开发者可将 Nova Index 作为自托管书签管理方案，替代浏览器内置书签，实现跨设备、跨浏览器的统一访问，并利用 Git 进行同步。

## 快速开始

以下步骤适用于初次部署 Nova Index 的开发者，建议在 Linux 或 macOS 环境下操作。

```bash
# 1. 克隆项目仓库
git clone https://github.com/novaindex/nova-index.git
cd nova-index

# 2. 安装依赖（仅需 Python 3.9+ 及 pip）
pip install -r requirements.txt

# 3. 运行本地开发服务器
python serve.py --port 8080
```

执行完毕后，访问 `http://localhost:8080` 即可查看默认索引首页。如需自定义资源列表，请编辑 `data/` 目录下的 Markdown 文件。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 及以上 | 核心运行时，用于本地预览与链接检查脚本 |
| Git | 2.25 及以上 | 版本控制，用于变更追踪与协作 |
| pip | 21.0 及以上 | Python 包管理，用于安装依赖库 |
| curl | 7.68 及以上 | 用于链接可用性检测脚本中的 HTTP 请求 |
| rsync | 3.2 及以上 | 可选，用于生产环境静态文件同步部署 |
| shellcheck | 0.7.0 及以上 | 可选，用于检测维护脚本中的 shell 语法问题 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户指南 | `docs/guide/` | 如何添加、修改或删除资源链接？索引分类标准是什么？ |
| 运维手册 | `docs/ops/` | 如何配置链接存活检测？如何导出索引为 JSON 或 HTML？ |
| 贡献规范 | `docs/contributing/` | 提交链接时需要遵循哪些字段格式？Commit Message 如何撰写？ |
| 设计说明 | `docs/design/` | 索引数据结构为何采用 YAML Front Matter？目录层级如何规划？ |
| API 参考 | `docs/api/` | 如何通过 HTTP 接口获取索引数据？查询参数含义是什么？ |

## 资源列表

以下为 Nova Index 默认收录的外部资源链接，按类别分组展示。所有链接均源自用户提供的原始数据，未做任何格式修改。

### 视频与影音资源

- <code>wuyetiantangchengren.com.cn</code>
- <code>wuyetiantangchengrendianying.com.cn</code>

### 社区与互动平台

- <code>bendiyueaisheququnzu.com.cn</code>
- <code>yueaishequzhenshiyonghu.com.cn</code>
- <code>yueaishequyuehuipingtai.com.cn</code>

### 综合资源导航

- <code>renqiwumazuixinziyuan.com.cn</code>

## 项目结构

```
nova-index/
├── data/                           # 索引数据主目录
│   ├── categories/                 # 分类定义文件（YAML）
│   │   ├── video.yaml              # 视频类分类规则
│   │   ├── community.yaml          # 社区类分类规则
│   │   └── general.yaml            # 综合类分类规则
│   ├── entries/                    # 资源条目目录（每个链接一个 .md 文件）
│   │   ├── 2026-08-25-example.md   # 示例条目，含 YAML Front Matter
│   │   └── templates/              # 条目模板文件
│   └── index.yaml                  # 全局索引配置（排序、分组策略）
├── scripts/                        # 维护与工具脚本
│   ├── check_links.py              # 链接存活检测脚本（基于 aiohttp）
│   ├── export_json.py              # 导出为 JSON 格式
│   └── generate_site.py            # 生成静态 HTML 预览站点
├── docs/                           # 项目文档（详见文档导航）
│   ├── guide/
│   ├── ops/
│   ├── contributing/
│   ├── design/
│   └── api/
├── tests/                          # 单元测试与集成测试
│   ├── test_entries.py             # 条目格式校验测试
│   └── test_scripts.py             # 脚本功能测试
├── serve.py                        # 本地开发预览服务入口
├── requirements.txt                # Python 依赖清单
├── .gitignore                      # Git 忽略规则
└── README.md                       # 本文件
```

## 贡献指南

欢迎通过 Issue 或 Pull Request 参与 Nova Index 的改进。请遵循以下步骤：

1. **查阅现有文档**：在提交新资源或修改前，请先阅读 `docs/guide/` 下的分类规范与字段说明，确保新增条目符合既有风格。

2. **创建条目文件**：在 `data/entries/` 目录下新建 Markdown 文件，文件名建议使用 `YYYY-MM-DD-资源简称.md` 格式。文件头部必须包含 YAML Front Matter，至少填写 `title`、`url`、`category`、`description` 四个字段。

3. **本地预览验证**：运行 `python serve.py` 启动本地服务，检查新条目是否正确显示在对应分类下，且链接可访问。

4. **编写测试用例**：若新增功能或修改脚本逻辑，请在 `tests/` 目录下补充相应的单元测试，确保覆盖率不低于 80%。

5. **提交 Pull Request**：提交时请附带清晰的 Commit Message，格式为 `<type>(<scope>): <subject>`，例如 `feat(entries): add video resource entry`。PR 描述中需说明变更动机与验证方式。

## 常见问题

**Q：Nova Index 是否支持动态搜索功能？**

A：Nova Index 核心设计为纯静态索引，不内置搜索服务。如需搜索能力，建议将导出的 JSON 数据集成至外部搜索引擎（如 Elasticsearch）或使用静态站点生成器的搜索插件。社区维护了一个可选扩展 `nova-search`，可基于 Lunr.js 实现前端本地搜索。

**Q：链接存活检测脚本会误报吗？**

A：检测脚本默认使用 HTTP HEAD 请求，超时时间为 10 秒。对于部分拒绝 HEAD 请求的站点，脚本会自动回退至 GET 请求并仅读取响应头。若站点有防火墙或反爬策略，可能导致误报，此时可将该链接加入 `scripts/whitelist.txt` 跳过检测。

**Q：如何批量导入现有书签数据？**

A：项目提供了 `scripts/import_bookmarks.py` 辅助脚本，支持导入 Chrome 导出的 HTML 书签文件（`bookmarks.html`）或 Firefox 的 JSON 备份文件。导入后需人工复核分类映射，因为自动映射无法保证完全准确。

## 许可证

MIT

> 外链数量: 6 | 生成时间: 2026-08-25 23:22:11
