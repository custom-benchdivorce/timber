# ResourceBridge

ResourceBridge 是一个面向技术社区与终端用户的开源资源导航与聚合平台。项目定位为轻量级、可自托管的「外链资源中台」，主要帮助开发者、技术内容创作者以及中小型社区运营者，解决分散链接难以管理、资源失效难以追踪、外链分发缺乏统一入口的痛点。ResourceBridge 本身不存储任何实体文件或数据内容，仅提供结构化的链接索引、状态探测与自定义分类视图，适用于需要快速搭建内部或公开资源导航页面的场景。

ResourceBridge 的核心设计理念是「只做资源的搬运工与登记员」。它通过可解析的配置文件（YAML / JSON）与静态站点生成逻辑，将用户提供的原始 URL 列表转化为带分类、标签、状态校验与访问统计的导航页面。项目内置定时链路检查（Link Health Check）功能，可定期对登记在册的外链进行可达性探测，并生成健康状态报表，帮助管理员及时发现失效资源。同时，ResourceBridge 支持多用户协作文档模式，允许团队共同维护资源目录，并记录每次增删改的操作日志，便于回溯与审计。

## 功能概览

- **集中式外链登记与管理**：提供统一的资源录入入口，支持批量导入原始 URL 列表，自动识别域名类别，并生成规范化存储记录。

- **链路健康检查与状态预警**：内置异步任务调度器，可按小时 / 天 / 周维度对全部已登记链接进行 HTTP 状态码校验，异常链接自动标记并推送预警通知（支持 Webhook 与邮件渠道）。

- **分类视图与自定义标签体系**：允许用户为每条资源分配多个标签（如“视频”、“社区”、“工具”），并基于标签或自定义分类规则动态生成导航页面，满足不同受众的浏览习惯。

- **静态站点生成与一键部署**：支持将资源数据渲染为纯静态 HTML 页面，无需数据库即可运行，可直接部署至 Nginx、S3 或 CDN 服务，适合高并发访问场景。

- **操作审计与版本回溯**：所有资源增删改操作均记录操作人、时间戳与变更内容，支持按时间范围回溯历史版本，便于团队协作与误操作恢复。

- **开放 API 与 Webhook 扩展**：提供 RESTful API 接口，支持外部系统调用，可实现与其他运维平台或自动化工具的联动，例如自动同步至企业 Wiki 或钉钉机器人。

## 应用场景

- **技术社区资源导航页**：技术社区或开源项目维护者可使用 ResourceBridge 快速搭建项目相关的周边资源导航，例如文档镜像、视频教程合集、第三方工具推荐等，帮助社区成员快速定位所需信息。

- **企业内部工具链登记**：企业内部运维或开发团队常面临大量内部系统地址（如 Jenkins、GitLab、监控面板）难以统一管理的困境。ResourceBridge 可作为内部工具链登记簿，提供清晰的分类视图和状态监控，减少“找链接”的时间成本。

- **内容创作者外链备案**：视频创作者或博客作者在作品中引用大量外部链接时，可使用 ResourceBridge 建立公开的“引用来源”页面，既提高内容透明度，也方便观众自行查阅，同时通过健康检查避免观众访问失效链接。

- **活动运营临时资源聚合**：在技术峰会、黑客松或线上直播活动中，运营人员可使用 ResourceBridge 快速生成活动专属资源合集，包含报名入口、资料下载、直播回放、交流群等链接，活动结束后可一键归档或下架。

- **个人书签管理升级版**：个人开发者可将 ResourceBridge 作为云端书签管理工具，替代浏览器自带的书签栏，实现跨设备、跨浏览器的统一访问入口，并享受失效链接自动提醒功能。

## 快速开始

以下步骤帮助您在本地环境快速启动 ResourceBridge 服务。

```bash
# 1. 克隆项目仓库
git clone https://github.com/resourcebridge/resourcebridge.git
cd resourcebridge

# 2. 安装项目依赖（基于 Python 3.10+）
pip install -r requirements.txt

# 3. 使用示例配置启动服务（默认监听 8080 端口）
python run.py --config config/sample.yaml --port 8080
```

启动成功后，访问 `http://localhost:8080` 即可查看示例资源导航页面。如需导入用户提供的原始链接列表，请将 URL 按行写入 `data/raw_links.txt` 文件，然后执行 `python scripts/import.py` 进行初始化导入。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Python | 3.10 及以上 | 项目核心运行环境，低于此版本将导致异步语法错误 |
| Pip | 21.0 及以上 | 用于安装 requirements.txt 中声明的第三方库 |
| Git | 2.25 及以上 | 用于克隆仓库及版本管理，非运行时强制依赖 |
| 操作系统 | Linux / macOS / Windows WSL2 | 推荐在类 Unix 环境下运行以获得最佳性能 |
| 网络连通性 | 需可访问公网 | 用于链路健康检查功能，若纯内网使用可关闭该功能 |
| 内存 | 最低 512MB，推荐 1GB | 静态站点生成模式下内存占用较低，若启用定时任务建议提高配置 |
| 存储 | 最低 100MB 可用空间 | 用于存储配置文件、日志及生成的静态页面，无数据库需求 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
|------|-----------|------------|
| 用户入门 | `docs/quickstart.md` | 如何快速安装并运行第一个导航实例？如何导入第一批链接？ |
| 配置参考 | `docs/configuration.md` | 配置文件中的每个字段代表什么含义？如何自定义分类和标签？ |
| 运维管理 | `docs/operations.md` | 如何配置链路健康检查的周期和通知方式？如何查看失效链接列表？ |
| 开发扩展 | `docs/development.md` | 如何通过 REST API 实现资源的增删改查？如何编写自定义插件？ |
| 部署指南 | `docs/deployment.md` | 如何将 ResourceBridge 部署至生产环境（Nginx + Systemd）？ |
| 常见问题 | `docs/faq.md` | 遇到端口占用、导入失败、检查超时等问题时如何处理？ |

## 资源列表

本部分汇总 ResourceBridge 项目默认配置中预置的示例资源链接，所有链接均来源于用户提供的原始数据，未作任何改写或格式化处理。

### 示例分类 A

<code>wuyetiantangchengrendianying.com.cn</code>

<code>bendiyueaisheququnzu.com.cn</code>

<code>renqiwumazuixinziyuan.com.cn</code>

### 示例分类 B

<code>yueaishequzhenshiyonghu.com.cn</code>

<code>yueaishequyuehuipingtai.com.cn</code>

<code>tongchengyueaishequjiaoyou.com.cn</code>

## 项目结构

```
resourcebridge/
├── run.py                           # 项目主入口，负责启动 Web 服务与调度器
├── requirements.txt                 # Python 依赖清单（Flask、requests、pyyaml 等）
├── config/
│   ├── sample.yaml                  # 示例配置文件，含分类、标签、检查周期等参数
│   └── schema.json                  # 配置文件 JSON Schema，用于 IDE 校验
├── core/                            # 核心业务逻辑模块
│   ├── loader.py                    # 资源加载器，解析 YAML/JSON 并构建内部索引
│   ├── checker.py                   # 链路健康检查引擎，包含异步 HTTP 探测与超时重试
│   ├── renderer.py                  # 静态页面渲染引擎，将资源数据转换为 HTML
│   └── auditor.py                   # 操作审计模块，记录 CRUD 日志并支持回滚
├── api/                             # RESTful API 接口层
│   ├── routes.py                    # 路由注册与请求分发
│   └── validators.py                # 请求参数校验器
├── scripts/                         # 运维辅助脚本
│   ├── import.py                    # 批量导入原始链接（支持纯文本和 CSV）
│   └── export.py                    # 导出资源列表为 JSON 或 Markdown 格式
├── templates/                       # Jinja2 模板目录，用于生成动态导航页面
│   ├── base.html                    # 基础模板，含公共样式与导航栏
│   └── index.html                   # 资源列表页模板，支持分类折叠与标签过滤
├── static/                          # 静态资源（CSS、JavaScript、图标）
│   ├── style.css                    # 响应式样式表，适配桌面与移动端
│   └── app.js                       # 前端交互逻辑，包括搜索过滤与状态刷新
├── data/                            # 数据存储目录（默认使用文件系统，不依赖数据库）
│   ├── resources.yaml               # 主资源存储文件，所有登记链接存放于此
│   ├── raw_links.txt                # 原始链接导入暂存区
│   └── audit.log                    # 操作审计日志，JSON Lines 格式
└── tests/                           # 单元测试与集成测试
    ├── test_checker.py              # 链路检查功能测试（含 Mock HTTP 响应）
    └── test_loader.py               # 资源加载与解析测试
```

## 贡献指南

我们欢迎并鼓励社区贡献，无论是问题报告、功能建议还是代码提交。请遵循以下流程：

1. **提交 Issue 进行需求沟通**：在 GitHub Issues 中详细描述您发现的问题或希望新增的功能，请附上复现步骤或使用场景说明。对于重大功能变更，建议先通过 Issue 讨论设计方案，避免重复劳动。

2. **Fork 仓库并创建特性分支**：从主仓库的 `main` 分支 Fork 到个人账户，然后基于 `main` 创建您的特性分支（如 `feat/add-tag-filter`）。分支命名建议遵循 `feat/`、`fix/`、`docs/` 前缀规范。

3. **编写代码并确保测试通过**：代码风格遵循 PEP 8，新增功能需补充对应的单元测试（位于 `tests/` 目录）。提交前请执行 `python -m pytest tests/` 确保所有测试用例通过，同时运行 `flake8` 进行静态检查。

4. **签署开发者原创声明（DCO）**：所有提交必须包含 `Signed-off-by` 行（可使用 `git commit -s` 自动添加），表示您有权贡献并同意本项目的开源许可条款。

5. **发起 Pull Request 等待审阅**：将特性分支推送到您的 Fork 仓库，然后向主仓库的 `main` 分支发起 PR。PR 描述中请关联对应的 Issue 编号，并简要说明变更内容与测试结果。项目维护者会在 3 个工作日内进行审阅。

## 常见问题

**Q：启动服务时提示“Address already in use”，如何解决？**

A：该错误表示默认的 8080 端口已被其他进程占用。您可以通过 `--port` 参数指定其他可用端口，例如 `python run.py --port 9090`。若需彻底释放端口，可使用 `lsof -i :8080` 查看占用进程 PID，然后使用 `kill -9 PID` 终止该进程（Linux/macOS）。Windows 用户可使用 `netstat -ano | findstr :8080` 查找 PID，并通过任务管理器结束进程。

**Q：导入大量链接后，健康检查任务执行非常缓慢甚至超时，如何优化？**

A：ResourceBridge 默认使用串行方式进行 HTTP 探测，对于超过 100 条链接的场景，建议调整配置中的 `checker.parallelism` 参数（位于 `config/sample.yaml`），将其值设置为 5 至 10 之间的整数，以启用并发检查。同时，您也可以增加 `checker.timeout` 值（单位秒）以避免网络延迟较大的链接被过早标记为失效。若仍无法满足需求，可考虑关闭实时检查，改为每日凌晨定时执行。

**Q：如何将 ResourceBridge 部署为系统服务实现开机自启？**

A：Linux 用户可参考 `docs/deployment.md` 中的 Systemd 配置示例，创建 `/etc/systemd/system/resourcebridge.service` 文件，并设置 `Restart=always`。Windows 用户可将 `python run.py` 添加为计划任务，或使用 NSSM（Non-Sucking Service Manager）将其包装为 Windows 服务。macOS 用户可使用 `launchd` 实现类似功能。

## 许可证

MIT

> 外链数量: 6 | 生成时间: 2026-08-25 23:22:11
