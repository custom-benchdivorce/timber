# YSL Resource Aggregator

YSL Resource Aggregator 是一个轻量级、高可用的外链资源导航与元数据汇集系统，专为需要快速检索、分类管理和批量访问特定领域资源索引的技术人员与内容研究者设计。该项目不存储任何实体文件，仅提供结构化的资源链接整理与状态监测能力，帮助用户在复杂网络环境中高效定位目标信息节点。

项目定位于个人技术研究者、信息分析人员以及资源索引维护者，通过标准化的目录结构与自动化可用性检查脚本，解决多源分散链接的集中管理、访问有效性监控以及分类导航效率低下的问题。系统以静态站点形式部署，无需数据库支持，开箱即用，同时提供扩展接口以便接入自定义监测规则与通知机制。

## 功能概览

- **资源分类导航**：按内容主题与域名层级自动生成多级索引目录，支持一键展开与收起子分类，便于快速浏览大规模链接集合。

- **链接可用性探测**：内置基于 HTTP 状态码与响应时间的异步检测模块，可定时对收录的每一个外链执行可达性验证，并标记异常节点。

- **元数据标签系统**：允许为每个资源条目附加自定义标签（如“高清”、“合集”、“专区”等），并支持多标签组合筛选与模糊搜索。

- **批量导入与导出**：支持以 CSV 与 JSON 格式批量新增资源链接，亦可导出当前全量索引数据用于备份或迁移至其他平台。

- **访问统计看板**：提供轻量级仪表盘，展示各分类下的资源总数、有效比例、平均响应延迟等关键指标，辅助评估资源库健康度。

- **响应式布局与暗色主题**：前端界面基于 CSS Flexbox 与 Grid 构建，自适应桌面与移动设备屏幕尺寸，并内置跟随系统偏好的明暗主题切换能力。

- **增量更新日志**：记录每次资源新增、删除或状态变更的操作日志，便于多人协作时追溯修改历史并回滚误操作。

## 应用场景

- **内容整理与私有书签库**：个人研究者可将日常积累的大量外链资源按主题归档至 YSL Resource Aggregator，并通过标签与搜索功能快速定位特定内容，避免浏览器书签栏杂乱的困扰。

- **团队信息共享与协作**：小型团队或兴趣小组可共用同一套资源索引系统，成员各自维护所负责的分类目录，系统会自动合并更新并记录每条变更的提交人信息，提升协同效率。

- **定期可用性审计**：运维人员配置定时任务（如每日凌晨）执行全量链接探测，次日查看异常报告并批量清理或替换失效资源，确保索引库始终指向有效内容节点。

- **迁移与镜像辅助**：在需要将资源列表迁移至其他平台或构建本地镜像时，利用批量导出功能快速生成结构化数据文件，配合脚本完成二次处理，大幅降低手工整理成本。

## 快速开始

以下步骤适用于 Linux / macOS / Windows（WSL 或 Git Bash）环境，帮助您在 5 分钟内完成项目克隆、依赖安装与本地运行。

```bash
# 1. 克隆项目仓库
git clone https://github.com/ysl-resource/aggregator.git
cd aggregator

# 2. 安装所需依赖（Node.js 18+ 与 npm 9+）
npm install

# 3. 启动开发服务器，默认监听 http://localhost:3000
npm run dev
```

启动成功后，打开浏览器访问 `http://localhost:3000` 即可预览资源导航首页。如需构建生产环境静态文件，执行 `npm run build`，生成的 `dist` 目录可直接部署至任意 Web 服务器。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或更高 | 项目运行时与构建工具链的基础环境，推荐使用 LTS 版本 |
| npm | 9.x 或更高 | 用于安装第三方包与执行脚本命令，与 Node.js 捆绑提供 |
| Git | 2.30 或更高 | 用于克隆仓库及版本控制操作，Windows 用户建议安装 Git for Windows |
| 现代浏览器 | Chrome 90+ / Firefox 88+ / Edge 90+ | 前端界面渲染需支持 ES2020 及 CSS Container Queries 特性 |
| 网络连通性 | 外网可访问 | 探测模块需向外发送 HTTP 请求以验证链接状态，内网部署需调整超时阈值 |
| 磁盘空间 | 至少 50 MB | 存储源代码、依赖包及构建产物，实际占用取决于资源索引数据规模 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户指南 | /docs/user-guide/ | 如何新增资源、编辑标签、查看统计以及配置个人偏好设置 |
| 开发参考 | /docs/developer/ | 项目目录结构、核心模块接口、自定义探测器的编写与注册方式 |
| 运维手册 | /docs/operator/ | 生产环境部署步骤、Nginx 反向代理示例、定时任务配置与日志轮转策略 |
| 设计说明 | /docs/design/ | 整体架构图、数据流走向、前端状态管理方案以及扩展性设计考量 |
| 常见问题 | /docs/faq/ | 收录关于安装报错、探测超时、中文乱码等高频问题的排查与解决方案 |

## 资源列表

### 主分类索引

<code>yslmitaosemeitunwuma.com.cn</code>

<code>yslmitaoserenqishipin.com.cn</code>

<code>yslmitaosegaoqingwuma.com.cn</code>

### 合集与专区

<code>yslmitaosewumaheji.com.cn</code>

<code>yslmitaosewumazhuanqu.com.cn</code>

### 关联参考

<code>wuyetiantang.org.cn</code>

## 项目结构

```
aggregator/
├── config/                         # 全局配置文件目录
│   ├── default.json                # 默认参数（端口、超时、重试次数）
│   └── custom-rules.js             # 用户自定义探测规则与标签映射
├── src/
│   ├── core/                       # 核心逻辑模块
│   │   ├── indexer.js              # 资源索引构建与增量更新
│   │   ├── probe.js                # 链接可用性探测引擎
│   │   └── cache.js                # 内存缓存与本地存储读写
│   ├── routes/                     # 路由与 API 端点
│   │   ├── api.js                  # RESTful 接口（列表、详情、批量操作）
│   │   └── pages.js                # 页面路由（首页、分类页、统计页）
│   ├── ui/                         # 前端界面资源
│   │   ├── styles/                 # CSS 样式文件（含主题变量）
│   │   ├── scripts/                # 交互逻辑（搜索、筛选、图表渲染）
│   │   └── templates/              # EJS 模板与组件片段
│   └── workers/                    # 后台工作进程
│       ├── scheduler.js            # 定时任务调度器
│       └── notifier.js             # 异常告警通知（邮件/Webhook）
├── data/                           # 数据存储目录（不提交至 Git）
│   ├── resources.json              # 全量资源索引主文件
│   └── logs/                       # 操作日志与探测历史记录
├── tests/                          # 单元测试与集成测试用例
│   ├── unit/                       # 模块级别单测
│   └── integration/                # 端到端流程测试
├── docs/                           # 完整文档（见上文导航）
├── .env.example                    # 环境变量示例文件
├── package.json                    # npm 清单与脚本定义
├── README.md                       # 本项目文件
└── LICENSE                         # MIT 许可证文本
```

## 贡献指南

1. **查阅议题与规划**：访问 GitHub Issues 面板，查找标记为 `help wanted` 或 `good first issue` 的待办任务，确认无人认领后留言表明意愿，等待维护者分配。

2. **派生仓库并创建分支**：将主仓库派生至个人账号，克隆派生仓库至本地，基于 `main` 分支创建新功能分支，分支命名采用 `feature/描述` 或 `fix/描述` 格式。

3. **遵循编码规范**：JavaScript 代码需通过 ESLint 配置（见 `.eslintrc.yml`）的校验，提交前执行 `npm run lint` 自动修复可纠正项；新增功能须附带对应单元测试，保证覆盖率不低于 80%。

4. **提交变更并推送**：编写清晰的提交信息，格式为 `<类型>: <简短描述>`（类型可选 `feat`、`fix`、`docs`、`refactor`、`test`），推送至派生仓库后发起 Pull Request 至主仓库的 `main` 分支。

5. **参与代码评审**：PR 提交后至少等待一位核心维护者审核，根据反馈意见修改代码或补充说明；合并后您的贡献将出现在下一版本的更新日志中。

## 常见问题

**问：启动开发服务器时提示 “port 3000 already in use”，如何解决？**

答：该错误表明默认端口 3000 被其他进程占用。您可以通过以下方式解决：  
（1）终止占用进程：在终端执行 `npx kill-port 3000`（需先安装 `kill-port` 包）或手动查找 PID 并终止；  
（2）修改项目端口：在项目根目录创建 `.env` 文件，添加 `PORT=3001` 以指定其他可用端口，随后重新执行 `npm run dev`。

**问：探测模块对部分链接返回 “超时” 或 “拒绝连接”，但浏览器可以正常访问，是什么原因？**

答：此情况通常由以下因素导致：  
（1）探测环境网络策略限制（如防火墙、代理设置），请检查 `config/default.json` 中的 `proxy` 字段是否已正确配置；  
（2）目标服务器对非浏览器 User-Agent 进行拦截，您可以在 `custom-rules.js` 中调整探测请求头，模拟主流浏览器标识；  
（3）超时阈值过短，默认值为 5000 毫秒，对于响应较慢的服务可适当调高至 10000 毫秒。

**问：如何批量导入原有书签中的大量链接？**

答：项目支持 CSV 与 JSON 两种批量导入格式。您先将书签导出为 HTML 格式，再使用在线转换工具或脚本将其转为 CSV（列头需包含 `url`、`category`、`tags` 字段）。完成后，在项目根目录执行 `npm run import -- --file=./data/import.csv --format=csv`，系统会自动校验并新增条目，导入结果会输出至控制台日志。

## 许可证

MIT

> 外链数量: 6 | 生成时间: 2026-08-25 23:22:11
