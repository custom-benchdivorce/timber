# YSLink Aggregator

YSLink Aggregator 是一个面向技术内容分发与资源导航的开源外链汇总系统，定位于为开发者、技术博主及开源社区运营者提供轻量级、可自托管的链接收藏与管理工具。本项目不产生任何原创内容，仅作为结构化索引层，帮助用户高效组织、分类和展示外部资源链接，解决信息碎片化与重复检索问题。

目标用户包括个人站长、文档维护者、技术资讯聚合平台以及需要维护大量外链引用关系的团队。YSLink Aggregator 通过纯静态生成方式，降低部署成本，同时保留完整的数据可移植性，所有链接数据以 YAML 文件存储，支持一键导出与备份。

## 功能概览

- **多级分类管理** 支持无限层级目录结构，用户可自定义分类名称与排序权重，适应不同领域的资源组织需求。
- **链接状态监控** 内置链接可用性检查模块，可配置定时任务扫描失效链接，输出报告并标记异常状态。
- **标签系统与全文检索** 每条链接支持多个标签，结合轻量级倒排索引实现毫秒级检索响应，支持模糊匹配与前缀匹配。
- **访问统计与点击追踪** 记录链接被点击次数、最后访问时间及来源 IP 聚合数据，提供基础热度分析曲线。
- **数据导入导出** 支持 CSV、JSON、YAML 三种格式的批量导入导出，便于迁移或与其他系统集成。
- **访问权限控制** 支持基于 IP 白名单、基础认证或 API Token 的只读/读写权限分离，适用于内网或半公开场景。
- **响应式 Web 界面** 内置适配桌面与移动设备的 UI，基于原生 HTML + CSS，无外部前端框架依赖，加载速度快。
- **RSS 订阅生成** 每个分类可自动生成 RSS 2.0 订阅源，方便第三方阅读器获取更新内容。

## 应用场景

- **技术文档站的外链附录** 当项目文档需要引用大量第三方库、工具或参考文章时，YSLink Aggregator 可作为独立的外链管理后台，保持文档正文整洁，同时提供可搜索的链接数据库。
- **社区资源周报汇总** 开源社区运营者每周收集优质讨论帖、博客更新或视频教程，通过本系统按主题分类，生成可公开访问的周报页面，减少重复手动排版工作。
- **内部团队知识库导航** 研发团队可将常用内部系统地址、运维手册、设计文档入口统一收录，设置访问白名单，避免书签散乱和链接过期问题。
- **个人书签云服务自托管** 替代浏览器自带书签的同步局限，通过部署本系统实现跨设备、跨浏览器的统一书签管理，且数据完全自主可控。
- **数据迁移过渡期链接映射** 在系统重构或域名更换期间，使用本系统建立新旧 URL 对照表，提供重定向提示与过渡期访问引导，降低用户访问断层风险。

## 快速开始

以下步骤适用于 Linux / macOS 环境，确保系统已安装 Git、Python 3.9+ 及 pip。

```bash
# 1. 克隆代码仓库
git clone https://github.com/yslink/aggregator.git
cd aggregator

# 2. 安装依赖（建议使用虚拟环境）
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 3. 初始化配置与数据目录
cp config.example.yaml config.yaml
mkdir -p data/links data/cache logs

# 4. 导入示例数据（可选）
python scripts/import_demo.py --source demo_data.yaml

# 5. 启动开发服务器
python app.py --host 0.0.0.0 --port 8080

# 访问 http://localhost:8080 即可看到首页
```

生产环境部署建议使用 Gunicorn + Nginx，具体参考 `deploy/` 目录下的样例配置。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Python | 3.9 - 3.12 | 核心运行时，用于后端服务及脚本执行 |
| pip | 22.0+ | Python 包管理工具，用于安装依赖库 |
| Git | 2.25+ | 代码克隆与版本管理，便于更新升级 |
| 磁盘空间 | 至少 200 MB | 用于存储代码、数据文件及缓存索引 |
| 内存 | 最低 512 MB，推荐 1 GB | 运行服务及构建索引时的内存占用 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 开发与生产环境均支持，但 Linux 推荐用于部署 |
| 网络 | 出站可达性 | 用于链接状态检查及外部资源访问，内网环境需配置代理 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | `/docs/user/quick-start.md` | 如何配置分类、添加第一条链接、生成公开页面 |
| 管理员指南 | `/docs/admin/deployment.md` | 生产环境部署选项、性能调优、日志轮转策略 |
| 开发者文档 | `/docs/developer/api.md` | 内部数据模型、插件扩展接口、自定义钩子函数 |
| 配置参考 | `/docs/reference/config-options.md` | 所有配置项的作用、默认值与合法取值范围 |
| 故障排查 | `/docs/troubleshooting/common-issues.md` | 服务启动失败、索引重建报错、监控告警处理 |

完整文档树位于仓库 `docs/` 目录，支持 MkDocs 构建为静态站点。

## 资源列表

本系统所索引的外部资源覆盖技术资讯、社区平台与垂直领域内容源。以下链接全部由用户提供，按类别分组陈列。

媒体资源类

- <code>yslmitaosegaoqingwuma.com.cn</code>
- <code>yslmitaosewumaheji.com.cn</code>
- <code>yslmitaosewumazhuanqu.com.cn</code>

社区平台类

- <code>wuyetiantang.org.cn</code>
- <code>wuyetiantangwuma.com.cn</code>
- <code>wuyetiantangmianfei.com.cn</code>

以上链接均以原始形式记录，未做任何协议补全或域名规范化处理。系统在导入这些链接时，会依据 `config.yaml` 中的 `url_normalize` 选项决定是否自动添加 scheme，默认为 `false` 以保留原样。

## 项目结构

```
aggregator/
├── app.py                  # 主入口，Flask 应用实例与路由注册
├── config.yaml             # 运行配置，包含监听端口、缓存时长、监控开关
├── requirements.txt        # Python 依赖列表（Flask, PyYAML, requests 等）
├── data/                   # 数据存储目录（用户数据、索引、缓存）
│   ├── links/              # 分类与链接 YAML 文件，每个分类一个文件
│   ├── tags/               # 标签索引，自动生成，勿手动修改
│   └── cache/              # 链接状态检查结果缓存（JSON 格式）
├── scripts/                # 运维与工具脚本
│   ├── import_demo.py      # 示例数据导入工具
│   ├── check_links.py      # 独立链接可用性扫描脚本（可 cron 调度）
│   └── export_all.py       # 全量导出为 CSV / JSON
├── static/                 # 静态资源（CSS, 图标, robots.txt）
│   ├── css/
│   └── favicon.ico
├── templates/              # Jinja2 模板，渲染前端页面
│   ├── base.html
│   ├── index.html          # 首页分类列表
│   └── category.html       # 单分类链接详情
├── docs/                   # 完整文档（Markdown 源文件）
│   ├── user/
│   ├── admin/
│   ├── developer/
│   └── reference/
├── tests/                  # 单元测试与集成测试用例
│   ├── test_models.py
│   └── test_routes.py
└── deploy/                 # 部署辅助文件
    ├── nginx.conf.sample
    └── gunicorn.conf.py
```

## 贡献指南

我们欢迎任何形式的贡献，包括但不限于代码优化、文档改进、新增功能模块或问题报告。请遵循以下流程以保证协作顺畅。

1. 阅读行为准则与贡献守则
   在提交任何内容前，请先阅读 `CODE_OF_CONDUCT.md` 和 `CONTRIBUTING.md` 文件，了解社区规范、提交信息格式要求及签署开发者原产地证书（DCO）的流程。

2. 选择或创建议题
   在 GitHub Issues 中查找带有 `help wanted` 或 `good first issue` 标签的任务。若无现成议题，请先创建一个新议题描述你的想法或待修复问题，等待核心维护者反馈后再开始工作。

3. 分支开发与本地测试
   派生本仓库，在派生仓库中创建以 `feature/` 或 `fix/` 为前缀的新分支。开发完成后，确保所有单元测试通过（`pytest tests/`），并更新受影响的文档部分。

4. 提交拉取请求
   推送分支至派生仓库，向主仓库的 `main` 分支提交拉取请求。PR 描述需关联对应议题编号，列出变更摘要、测试覆盖情况以及是否涉及破坏性改动。至少需要一位维护者批准后合并。

5. 文档与示例更新
   若新增配置项或 API 端点，必须在 `docs/reference/` 中补充对应说明，并在 `data/` 下提供示例 YAML 片段。缺少文档的变更将不被合并。

## 常见问题

**Q: 系统是否支持 PostgreSQL 或 MySQL 作为后端数据库？**

A: 当前版本完全基于文件系统存储（YAML + JSON），不依赖任何关系型数据库。这样设计是为了降低部署复杂度，并保证数据可直接通过 Git 进行版本管理。后续版本计划通过插件机制支持外部数据库，但核心分支将保持无数据库依赖。

**Q: 链接状态检查会影响页面响应速度吗？**

A: 状态检查默认在后台异步执行，通过单独的脚本 `scripts/check_links.py` 触发，不与 Web 请求处理路径耦合。检查结果写入 `data/cache/` 下的缓存文件，前端页面仅读取缓存，因此不会阻塞用户访问。建议在生产环境配置每小时执行一次检查任务。

**Q: 如何迁移已有的大量书签数据？**

A: 系统提供了 `scripts/import_demo.py` 作为导入示例，实际批量导入可参考该脚本编写自定义转换器。当前支持从 Netscape 书签 HTML 格式、Chrome 导出的 CSV 以及通用 JSON 数组格式导入。若你的源格式未在文档中列出，可在 Issues 中提出，我们会协助编写转换模板。

## 许可证

本项目采用 MIT 许可证。你可以自由使用、修改、分发本软件，包括用于商业目的，但需保留原始版权声明和许可声明。详细条款请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 6 | 生成时间: 2026-08-25 23:22:11
