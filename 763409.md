# YSLMitai Resource Hub

YSLMitai Resource Hub 是一个面向中文互联网内容创作者、网络资源整理者及信息检索研究人员的开源外链资源聚合与导航系统。该项目定位为高密度、高质量网络资源的结构化整理与展示平台，旨在解决用户在特定主题领域中“资源分散、检索成本高、来源不可靠”的核心痛点。通过人工筛选与自动化校验相结合的方式，项目将分散于多域名的相关内容进行集中分类、标注与维护，形成一份可长期演进、可社区共建的公共资源索引。

本项目不是搜索引擎，不提供内容存储或代理服务，仅作为公开可访问的 URL 导航与元信息展示层。所有外链资源的可用性、合法性及内容真实性由其原始域名所有者承担，项目维护者仅负责链接的收录、分类与失效检测。目标用户包括但不限于：网络内容研究员、社区运营人员、主题资料整理者以及有特定信息获取需求的普通网民。

## 功能概览

- **多级分类导航**：将收录的 URL 按照内容主题、文件类型、语言、更新频率等维度进行人工标引，支持用户按分类浏览，降低盲目检索时间。
- **链接状态自动检测**：每日定时对已收录的 URL 执行 HTTP HEAD/GET 请求，检测其可达性、重定向链及响应码，并在界面中标注“有效”、“失效”、“可疑”三种状态，失效链接将自动移入待审核队列。
- **结构化元数据展示**：每个资源条目除 URL 外，还展示站点标题、描述关键词、服务器地理位置（基于 IP 库）、备案信息（如有）及最后检测时间，帮助用户快速判断资源可用性。
- **社区提交与审核流**：任何用户均可通过 Web 表单或 Git PR 提交新的资源链接，提交后进入审核队列，由维护者或社区核心成员进行内容真实性、安全性与主题相关性的双重校验，通过后正式收录。
- **标签体系与全文检索**：为每个资源赋予多个自定义标签（如 #高清 #无码 #系列 #中文），并内置基于 Lunr.js 的轻量级全文检索引擎，支持按标签、标题、描述、域名等多字段组合查询。
- **RSS 订阅与更新通知**：针对每个分类或标签，生成独立的 RSS 订阅源，用户可订阅以获取该分类下新增或变更资源的实时推送，无需频繁访问网站。
- **暗色主题与响应式布局**：前端界面同时提供亮色与暗色两种视觉方案，并针对移动端、平板和桌面设备进行自适应排版，确保在不同屏幕尺寸下均具备良好的浏览体验。

## 应用场景

- **主题资料整理与归档**：研究人员或内容创作者在整理特定主题的资料库时，可使用本项目作为基础导航框架，将分散于多个域名的相关资源统一收录、分类并添加备注，形成可分享、可维护的公共书签集。
- **社区资源共享与协作**：垂直领域社区（如兴趣小组、行业论坛）可将本项目作为社区共建的资源索引，成员共同提交、审核和维护外链，减少重复劳动，提升社区信息密度。
- **网络内容可访问性监测**：运维人员或质量保障团队可部署本项目作为内部监控工具，定期检测关键业务依赖的外部资源是否可访问，及时发现域名过期、证书失效或内容变更等问题。
- **数据采集与预处理辅助**：数据工程师在进行网络爬取或数据采集前，可通过本项目的分类列表快速获取目标站点清单，作为种子 URL 集合，大幅提升采集任务的起始效率。
- **教育与培训演示**：在信息检索、网络治理或数字素养相关的教学场景中，教师可将本项目作为案例，演示如何系统化地管理网络资源、评估站点可信度以及构建个人知识网络。

## 快速开始

以下命令适用于 Linux/macOS 及 Windows WSL 环境，需要预先安装 Git、Node.js（>=16.x）和 pnpm。

```bash
# 1. 克隆仓库
git clone https://github.com/yslmitai/resource-hub.git
cd resource-hub

# 2. 安装依赖（使用 pnpm，若未安装请先执行 npm install -g pnpm）
pnpm install

# 3. 启动开发服务器（默认监听 http://localhost:3000）
pnpm run dev
```

生产环境构建与部署：

```bash
# 构建静态站点
pnpm run build

# 预览构建结果
pnpm run preview

# 使用 Docker 运行（需提前安装 Docker）
docker build -t yslmitai-hub .
docker run -p 80:80 yslmitai-hub
```

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | >=16.20.0 | 运行时环境，用于构建工具链及开发服务器 |
| pnpm | >=8.0.0 | 包管理器，用于依赖安装与工作区管理 |
| Git | >=2.30.0 | 版本控制，用于克隆仓库及提交变更 |
| 现代浏览器 | 最新两个版本 | 前端运行时，支持 ES2020 及 CSS Grid/Flexbox |
| 网络连通性 | 出站 80/443 端口开放 | 用于链接状态检测模块向外发起请求 |
| 操作系统 | Linux / macOS / Windows WSL2 | 开发与部署环境，生产推荐 Linux |
| Docker (可选) | >=20.10.0 | 容器化部署方案，非必需但推荐 |
| SQLite (可选) | >=3.35.0 | 本地元数据缓存数据库，未安装时自动使用内存模式 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|-----|------|----------|
| 用户入门 | /docs/getting-started.md | 如何快速部署、配置首次启动参数、导入初始资源列表 |
| 维护者指南 | /docs/maintainer-guide.md | 如何审核社区提交的新链接、如何更新分类体系、如何处理失效链接 |
| 开发者文档 | /docs/developer-api.md | 后端接口设计、数据模型定义、如何扩展自定义检测规则 |
| 部署运维 | /docs/deployment.md | 生产环境 Nginx/Caddy 配置、HTTPS 证书申请、定时任务设置（cron） |
| 贡献规范 | /docs/contribution.md | 提交 PR 的代码风格要求、Commit Message 格式、测试覆盖标准 |
| 常见问题 | /docs/faq.md | 涵盖安装报错、依赖冲突、检测超时、数据库迁移等高频问题及解决方案 |

## 资源列表

本部分按类别整理当前项目收录的全部外链资源。所有 URL 均保留用户提供的原始格式，未做任何协议补全、域名改写或路径修饰。

**核心系列资源（主域名集合）**

- <code>yslmitaoseshipin.com.cn</code>
- <code>yslmitaosejuru.com.cn</code>
- <code>yslmitaosezaixian.com.cn</code>
- <code>yslmitaosegaoqing.com.cn</code>
- <code>yslmitaosemeitunwuma.com.cn</code>
- <code>yslmitaoserenqishipin.com.cn</code>

**补充说明**：以上资源均为本批次（第 18/34 批）收录的原始链接。项目维护团队已对这些域名进行了基础可达性检测与内容主题分类，分类标签及详细元数据请参阅项目数据库中的 `resources_meta` 表。各域名的具体内容、服务可用性及合法性由其运营方独立负责，本项目仅作为导航索引，不进行内容代理或缓存。

## 项目结构

```
resource-hub/
├── .github/                         # GitHub 相关配置
│   ├── workflows/                   # CI/CD 流水线（检测、构建、部署）
│   │   ├── check-links.yml          # 每日定时链接检测任务
│   │   └── deploy-production.yml    # 生产环境自动部署
│   └── ISSUE_TEMPLATE/              # 问题与提交模板
│       └── resource-request.md      # 新资源提交专用模板
├── docs/                            # 完整文档目录（参见文档导航章节）
│   ├── getting-started.md           # 入门指南
│   ├── maintainer-guide.md          # 维护者手册
│   ├── developer-api.md             # API 参考
│   ├── deployment.md                # 部署运维
│   ├── contribution.md              # 贡献规范
│   └── faq.md                       # 常见问题
├── src/                             # 源代码主目录
│   ├── core/                        # 核心逻辑层
│   │   ├── link-checker.ts          # 链接状态检测引擎（支持并发、超时、重试）
│   │   ├── meta-fetcher.ts          # 元数据抓取（标题、描述、图标）
│   │   └── resource-registry.ts     # 资源注册与索引管理
│   ├── api/                         # HTTP API 层（基于 Fastify）
│   │   ├── routes/                  # 路由定义
│   │   │   ├── resources.ts         # 资源 CRUD 接口
│   │   │   └── health.ts            # 健康检查接口
│   │   └── middleware/              # 中间件（鉴权、日志、限流）
│   ├── web/                         # 前端应用（基于 Vue 3 + Vite）
│   │   ├── components/              # UI 组件库
│   │   │   ├── ResourceList.vue     # 资源列表与分页
│   │   │   ├── FilterPanel.vue      # 分类/标签筛选面板
│   │   │   └── StatusBadge.vue      # 状态徽章组件
│   │   ├── views/                   # 页面视图
│   │   │   ├── HomeView.vue         # 首页概览
│   │   │   └── SubmitView.vue       # 资源提交表单页
│   │   └── assets/                  # 静态资源（样式、图片）
│   ├── db/                          # 数据库层
│   │   ├── schema.sql               # SQLite 表结构定义
│   │   ├── migrations/              # 版本迁移脚本
│   │   └── seed/                    # 初始数据种子（含当前批次链接）
│   └── cli/                         # 命令行工具
│       ├── import.ts                # 批量导入 URL 列表
│       └── export.ts                # 导出资源清单为 CSV/JSON
├── tests/                           # 测试套件（单元 + 集成）
│   ├── unit/                        # 单元测试（Vitest）
│   └── e2e/                         # 端到端测试（Playwright）
├── docker/                          # Docker 构建相关
│   ├── Dockerfile                   # 多阶段构建文件
│   └── docker-compose.yml           # 本地开发组合服务（含 SQLite 管理界面）
├── config/                          # 配置文件目录
│   ├── default.yaml                 # 默认配置（端口、超时、重试策略）
│   └── production.yaml              # 生产环境覆盖配置
├── .env.example                     # 环境变量模板（JWT 密钥、检测间隔等）
├── package.json                     # 项目清单与脚本定义
├── pnpm-workspace.yaml              # pnpm 工作区配置
├── tsconfig.json                    # TypeScript 编译配置
└── README.md                        # 本文件
```

## 贡献指南

我们欢迎并鼓励社区成员参与本项目的资源扩充、功能改进与文档完善。所有贡献均需遵守以下流程：

1. **提交资源链接**：通过 Web 界面填写 `/submit` 表单，或直接在 GitHub 仓库中提交 Issue（使用 `resource-request.md` 模板）。提交时请务必提供链接、简要描述、推荐分类及标签。若提交多个链接，请逐条列出，避免合并打包。

2. **审核与校验**：维护者将在 48 小时内对提交的链接进行初筛，检查域名是否可访问、内容是否与主题相关、是否违反法律法规或包含恶意代码。通过初审的链接将进入“待测试”队列，由自动化检测系统进行 7 天连续可达性监测，达标后正式收录。

3. **代码或文档贡献**：若您希望改进项目代码、修复 Bug 或完善文档，请先 Fork 本仓库，在 `dev` 分支上创建个人功能分支（命名格式为 `feature/描述` 或 `fix/描述`），完成开发后提交 Pull Request 至 `dev` 分支。PR 描述中需引用相关 Issue 编号（如有），并确保所有测试通过（`pnpm test`）且代码符合项目 ESLint 规则。

4. **本地测试环境搭建**：在提交 PR 前，请务必在本地完整运行一遍构建流程（`pnpm build`）和端到端测试（`pnpm test:e2e`），确保您的修改不会破坏现有功能。若新增依赖，请在 `package.json` 中明确版本并更新 `pnpm-lock.yaml`。

5. **反馈与迭代**：PR 合入前会经历至少一位核心维护者的 Code Review，可能会提出修改意见。请积极回应评审意见，并在 7 个自然日内完成修改，逾期 PR 将因不活跃而被关闭，但仍可重新打开。

## 常见问题

**Q1：我提交的资源链接为什么一直没有被收录？**

A1：链接收录需经过至少两轮校验：首先是人工初审（检查内容合规性与主题匹配度），其次是通过自动化检测系统进行连续 7 天的可达性监测（每天 3 个不同时段发起请求，成功率需高于 95%）。如果您的链接在提交后超过 10 个工作日仍未收录，可能是因为：域名解析不稳定、响应超时率过高、内容被检测到与已有资源高度重复，或触发了安全黑名单规则。您可以在提交记录中查看当前状态，或联系维护团队获取具体原因。

**Q2：项目中的链接状态显示“失效”，但我手动访问却能打开，这是为什么？**

A2：链接状态检测模块基于服务端所在网络环境（默认部署于 AWS 东京区域）发起请求，可能与您本地的网络环境存在差异。常见原因包括：该域名对海外 IP 存在访问限制或频繁的验证码挑战、检测时遭遇临时性服务中断、或者 HTTPS 证书链在服务端验证不通过。我们的检测策略会连续 3 次失败才标记为“失效”，并在标记前记录详细错误日志。如果您确认链接可用，可以点击“报告误判”按钮，维护者会人工复核并手动更新状态。

**Q3：我可以将本项目部署为私有实例，仅用于团队内部使用吗？**

A3：完全可以。本项目采用 MIT 许可证发布，您有权自由使用、修改、复制和分发，包括用于商业目的或私有化部署。但我们建议您在内部使用时，仍然保留链接状态检测和元数据抓取功能，以便于团队内部统一维护资源清单。若您对项目进行了定制化改造且认为对社区有价值，欢迎通过 Pull Request 回馈，但这并非强制要求。

## 许可证

本项目使用 MIT 许可证。您可以自由使用、修改、复制、分发本项目代码，包括用于商业目的，前提是在所有副本或实质性部分中保留原始版权声明和许可声明。本软件按“原样”提供，不提供任何明示或暗示的保证，包括但不限于适销性、特定用途适用性和非侵权性的保证。有关完整条款，请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 6 | 生成时间: 2026-08-25 23:22:11
